import { API } from './API.js'
import { CACHE } from './CACHE.js'

const PUBLIC_PATH = './3d'
const initialState = {
  position: [
    -1.39,
    139.18,
    156
  ],
  target: [
    -1.29,
    0,
    42.86
  ]
}

export const STATE = {
  initialState,
  PUBLIC_PATH,
}
